const numbers = [1, 2, 3, 4, 3];

const index = numbers.indexOf(3); //it will return index of that element
console.log(index);

const lastIndex = numbers.lastIndexOf(3); // it will return last index of that element
console.log(lastIndex);

const isThere = numbers.includes(1);
// it will return true if that element is exists in array
console.log(isThere);


