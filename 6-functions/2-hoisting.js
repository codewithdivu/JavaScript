// Hoisting -> JS engine will move all the function declaration at the top of code so it is called hoisting

// hoisting is the process of moving function declration at the top of the file

// example

walk();

function walk() {
  console.log("walk");
}
